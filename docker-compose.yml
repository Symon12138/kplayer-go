services:
  kplayer:
    build: .
    ports:
      - "4155:4155"
      - "4156:4156"
    volumes:
      - ./data:/kplayer/data          # 业务数据（management.json 等）
      - ./video:/kplayer/video        # 媒体文件
      - ./config.json:/kplayer/config.json
    restart: unless-stopped